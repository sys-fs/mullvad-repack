#!/usr/bin/env /lib/runit/invoke-run
MULLVAD_RESOURCE_DIR="/opt/Mullvad VPN/resources/"

exec 2>&1
if [ -e /etc/runit/verbose ]; then
	echo "invoke-run: starting ${PWD##*/}"
fi
exec /usr/bin/mullvad-daemon --initialize-early-boot-firewall
